// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://Atotti.github.io/astro-portfolio',
  base: '/astro-portfolio',
  outDir: 'dist',
})
